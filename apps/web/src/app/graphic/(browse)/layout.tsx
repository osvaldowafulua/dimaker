import { GraphicBrowseShell } from '@/components/graphic/graphic-browse-shell';

export default function GraphicBrowseLayout({ children }: { children: React.ReactNode }) {
  return <GraphicBrowseShell>{children}</GraphicBrowseShell>;
}
